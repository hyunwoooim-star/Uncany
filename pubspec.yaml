name: uncany
description: "학교 커뮤니티 플랫폼 - 교사들을 위한 리소스 예약 시스템"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # 상태 관리
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # 라우팅
  go_router: ^14.0.2

  # 백엔드 (Supabase)
  supabase_flutter: ^2.3.4

  # 불변 모델
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

  # UI/UX
  flutter_animate: ^4.5.0
  google_fonts: ^6.1.0
  shimmer: ^3.0.0
  flutter_staggered_animations: ^1.1.1

  # 유틸리티
  intl: ^0.20.2
  uuid: ^4.3.3
  crypto: ^3.0.3

  # 파일 선택
  file_picker: ^8.0.0

  # 이미지 압축
  image: ^4.1.0

  # 📱 모바일 전용 의존성 (Web에서는 무시됨)
  permission_handler: ^11.3.0  # 권한 관리 (Android 13+ 대응)
  device_info_plus: ^10.1.0    # 디바이스 정보 (Android 버전 확인)
  uni_links: ^0.5.1            # Deep Link 감지
  sign_in_with_apple: ^5.0.0   # 애플 로그인 (iOS 필수)
  image_picker: ^1.0.7         # 카메라 촬영

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 린터
  flutter_lints: ^3.0.1

  # 코드 생성
  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.7.1
  riverpod_generator: ^2.4.0

  # 📱 모바일 에셋 생성
  flutter_launcher_icons: ^0.13.1  # 앱 아이콘 자동 생성
  flutter_native_splash: ^2.3.10   # 스플래시 화면 자동 생성

# ====================================
# 앱 아이콘 설정 (토스 스타일)
# ====================================
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/Uncany.png"
  min_sdk_android: 21
  adaptive_icon_background: "#3182F6"  # 토스 블루
  adaptive_icon_foreground: "assets/images/Uncany.png"
  # 실행: dart run flutter_launcher_icons

# ====================================
# 스플래시 화면 설정 (토스 스타일)
# ====================================
flutter_native_splash:
  color: "#3182F6"  # 토스 블루
  image: assets/images/Uncany.png
  android_12:
    image: assets/images/Uncany.png
    color: "#3182F6"
  ios: true
  android: true
  web: true
  # 실행: dart run flutter_native_splash:create

flutter:
  uses-material-design: true

  # 폰트는 google_fonts 사용

  assets:
    - assets/images/
    - assets/images/Uncany.png
